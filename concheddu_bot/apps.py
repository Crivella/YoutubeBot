"""App configuration for the concheddu_bot app."""
from django.apps import AppConfig


class ConchedduBotConfig(AppConfig):
    """App configuration for the concheddu_bot app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'concheddu_bot'
